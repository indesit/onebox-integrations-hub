# Поточні задачі (Next Steps)

## 1. Зафіксувати ETL для нових endpoint-ів
- [x] Додати staging та dim-моделі для `stores`.
- [x] Додати staging та dim-моделі для `customers`.
- [x] Налаштувати джоби поллінгу для `/stores` і `/customers` (`stores_customers_sync.py`).

## 2. Побудувати Enriched Layer
- [x] Створити SQL-view або функцію на рівні БД, яка об'єднує (реалізовано як SQLAlchemy JOIN у `OneBoxSyncWorker`):
  - `fact_sales_receipt_items`
  - `dim_product_variants`
  - `dim_stores`
  - `dim_customers`
- [x] Забезпечити, щоб OneBox Worker читав дані виключно з цього збагаченого шару.

## 3. Переписати OneBox Sync Worker
- [x] Брати чек як одну транзакцію (групування по `receipt_uuid`).
- [x] Підтягувати з Enriched Layer телефон клієнта, назву магазину, правильний артикул (з суфіксом) та назви.
- [x] Після успішного `order/set/` ставити `status = synced`, `synced_at`, `order_id`.
- [x] Формат назви угоди: лише індекс і номер (напр. `W04760`).

## 4. Дебаг передачі товарів у OneBox
- [x] Вирішити проблему з дублюванням товарів в існуючому замовленні (`deletenotupdatedproducts: True`).
- [x] Переконатися у правильності формату `productinfo` та ключа `product id` у запиті до OneBox.

## 5. Додатково
- [x] Мапінг магазину в поле `UF_CRM_1526724731` (текстові назви).
- [ ] Повноцінна підв'язка покупця (Tomorrow's goal).
- [ ] Мапінг клієнтів (якщо будуть інші параметри).

## 6. Розширення DWH (Аналітика та P&L)
- [x] Оновлення схеми `DimProductVariant`: додати `characteristic_article` та додаткові реквізити (`tip`, `material`, `osobennosti` тощо).
- [x] Перехід на використання `characteristic_article` як основного ключа для OneBox Sync (відмова від парсингу імен).
- [x] Модифікація ETL каталогу товарів для розбору нових полів з BAF API.
- [x] Повний перерахунок каталогу товарів (33k+ записів) для наповнення новими атрибутами.

## 7. Наступні кроки (Phase 2)
- [ ] **Фінансова звірка:** Інтеграція видаткових накладних (ВН) або банківських виписок для P&L.
- [ ] **Автоматизація залишків:** Синхронізація складських залишків `stock_balance_now` із OneBox (потребує UUID матчингу).
- [ ] **Dashboarding:** Підключення BI-інструменту (напр. Metabase або Superset) до нашого PostgreSQL DWH.

## 8. План на сьогодні (2026-03-14)
- [ ] **Фінальний тест-драйв:** Прогін синхронізації всього денного потоку чеків, перевірка лінкування клієнтів та артикулів у реальному часі.
- [ ] **Автоматизація (Cron):** Налаштування регулярного запуску воркерів (опитування BAF, синхронізація OneBox, оновлення довідників).
- [ ] **Telegram Bot:**
  - Налаштування сповіщень про нові чеки (сума, магазин, клієнт).
  - Команда `/stats` для отримання оперативної виручки за день.
  - Сповіщення про помилки синхронізації.
